# identity-resolution
Built a graph-based identity resolution pipeline using probabilistic record linkage, multi-pass blocking, and entity clustering. Compared rules, logistic regression, random forest, and XGBoost for duplicate detection, then constructed record- and attribute-level identity graphs linking records, emails, phones, and addresses.
